# Daikin XML Listener Configuration
# Copy this file to .env and update with your values

# Required S3 Configuration
BUCKET_NAME=your-s3-bucket-name
AWS_REGION=us-west-2

# AWS Credentials (for testing only - use secrets in production)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# Network Configuration
PORT=8080
BIND_HOST=0.0.0.0

# File Management
# For near-real-time business intelligence, use 60 second intervals
ROTATION_INTERVAL=60        # Rotate every 60 seconds for real-time BI
MAX_FILE_SIZE=5242880       # 5MB max file size (5 * 1024 * 1024 bytes)
OUTPUT_FORMAT=json          # json recommended for Snowflake performance
PREFIX=xml-events/
USE_DATE_FOLDERS=false
PRETTY_PRINT_JSON=false     # false for production (smaller files, faster parsing)

# Note: Files rotate on EITHER condition being met first:
# - Every 60 seconds (for real-time data availability)
# - When file reaches 5MB (to prevent large file handling issues)

# Security Settings - Production recommendations
MAX_CONNECTIONS=50          # Increased for production load
MAX_MESSAGE_SIZE=1048576    # 1MB in bytes per event
RATE_LIMIT_ENABLED=false    # Disable for trusted ACM appliances
RATE_LIMIT_WINDOW=60        # seconds (if rate limiting enabled)
RATE_LIMIT_MAX_EVENTS=10000 # High limit for production (if enabled)

# File Paths (used internally by container)
CURRENT_FILE=/app/data/current.xml
TEMP_FILE=/app/data/temp.xml